PI = 3.14
V = 0
S = 0
r = 0
h = 0

r = float(input('Enter radius: '))
h = float(input('Enter height: '))

if (r <= 0 or h <= 0) : print("Values must be higher than 0"); return;

S = 2 * PI * r * (r + h)
V = PI * r * r * h

print("Surface area: %.2f" %S)
print("Volume: %.2f" %V)
